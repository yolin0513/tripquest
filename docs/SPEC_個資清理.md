# SPEC_個資清理 — 普查找到的真實出遊日期與本機路徑，從最新版拿掉；git 歷史不改寫

> 狀態：已執行（2026-09-23、v1.74.7）
> 撰寫：統籌 Session（Opus 5.5，effort Max），2026-09-23。依據：你 2026-09-23 的個資普查回報（Dispatch 轉達）＋統籌者對 `a547a1f` 的唯讀查證（行號以你讀到的為準）。
> 建議 effort：medium（改文字、改腳本裡的日期字面、重產截圖；不動 App 的程式邏輯與資料結構）。
> **這一份本身不寫任何真實日期、行程名稱、群組 ID、地址——只寫檔名、行號與類型。請你照做：STATUS、commit 訊息、回報都不要貼出原本的值。**

## 一句話

普查找到一組已公開的真實出遊日期（連續三天，共 11 處）與 4 處本機路徑。這一份把它們從最新版拿掉：測試照樣綠、沒有任何斷言因此放寬、截圖照樣示範它原本要示範的東西。**git 歷史不改寫**（改寫已推送的歷史要 Yolin 點頭，而且 GitHub 的舊物件用完整 SHA 仍撈得到）——這一點要照實寫進 STATUS。

## 0. 不在這一份的（Dispatch 已先授權、你正在做）

邀請連結截圖 `screenshots/features/v1580-邀請連結-LINE顯示長度對比.png` 換成合成的示意圖；STATUS 記「真實的群組祕鑰可能曾入過這張圖、git 歷史裡仍在」。這一份不再碰它；要不要換祕鑰見 §6 的 Q5。

## 1. 現況（統籌者 2026-09-23 唯讀查證）

- **行程檔** `scripts/fixtures/yilan.txt` 第 3、21、33 行是「第#天 (年/月/日)」三行，連續三天，**沒有寫星期幾**。
- 用到這個檔的：測試 `itintest`、`importtest`、`plantest`；截圖 `importshots`、`v148shots`。**三支測試斷言的都是結構**（旅程名稱、景點筆數、沒有讀不懂的行、分成 3 天、停留分鐘數、店名清理、兩筆「家」）——**沒有任何一條比對日期的值，也沒有任何測試用到解析出來的日期**。
- **解析器** `js/itinerary.js`：「第N天」後面的日期照同一種格式處理；另有「整行幾乎只剩日期就當換天」的規則（`itintest` 的「日期換天」那幾條靠它）。所以新的日期必須：同一種格式、日曆上真的存在、連續三天、順序不變。
- `scripts/shotxl.mjs` 約 37、`scripts/v148shots.mjs` 約 40：截圖用的旅程 `startDate`／`endDate` 寫死成那三天。
- `css/style.css` 約 1518：一段註解轉述實機回報，含起訖日期與人數。
- **六張圖**：
  - `screenshots/features/v1735-回顧日期列-390px特大字-修正前.png`／`-修正後.png`：找不到產生它們的腳本；`affectedtest` 只把這個**檔名**當歷史資料引用（所以不能改名，內容可以換）。
  - `screenshots/_v148/海報-最後一張-下一張已藏.png`、`海報-第一張-前一張已藏.png`：`v148shots.mjs` 產生（它會打真的 Nominatim）。
  - `screenshots/_import/v1.38-03-paste-text.png`：`importshots.mjs` 產生；`screenshots/features/v1.38-貼上真實行程表.png` 是它的複本。
- **本機路徑（不含使用者名稱）**：`SETUP_TODO.md` 43（說明文字裡的 `cd`）、`docs/STATUS.md` 1455、`scripts/publish-cloud.mjs` 4 與 `scripts/publish-cloud.sh` 4（兩支那一行都是**用法註解**，不是會執行的程式）。

## 2. 分類用的定義（普查工單第三版＋修訂一＋這一份補的四條）

修訂一（JLPT_App 的回報促成；你做普查時還沒拿到，這裡一併給）：
- **本機路徑分三級**：含使用者名稱或家目錄 → 算個資；只有磁碟機加專案路徑 → 不算個資、列「清理建議」；每台電腦都一樣的系統路徑 → 不算、不列。
- **使用紀錄**限定 Yolin 或家人的真實紀錄；開發或測試自己跑出來的數字不算。
- **已經寫在公開 commit 作者欄的帳號名稱**不算（本來就公開，改檔案拿不掉）。

這一份補的（依你提的四個問題）：
- **(a) 真實行程的名稱＋真實日期 → 算**（第 2 類「誰在哪一天在哪裡」）；名稱單獨不算——所以行程檔裡的旅程名稱與景點可以留著，日期換掉就不再指向那一次出遊。
- **(b) ID 的前綴**：拿不來加入或讀取資料，不是憑證；但它仍對得到那一組家人 → 列「清理建議」，要不要清**待 Yolin**（§6 Q1）。
- **(c) 看起來像憑證、但解不開的字串**：只要不能證明是假的，就當真的處理——從最新版換掉（跟邀請連結截圖同一個理由：換掉幾乎零成本，萬一是真的代價很大）；歷史不改寫。
- **(d) 家人同行的統計數字**（公里數、任務數、照片數、人數）：單獨出現不算；跟真實日期或真實行程名稱連在一起，就歸第 2 類一起處理。

## 3. 目標與非目標

**目標**：G1 最新版裡不再有那三天的日期（11 處全清）；G2 測試照樣綠，**沒有任何斷言被放寬、改成不看值、或拿掉**；G3 每張換掉的圖照樣示範它原本要示範的東西；G4 4 處本機路徑改成相對的說法。

**非目標**：不改寫 git 歷史；**不刪任何已 commit 的檔**（刪已 commit 的檔要先問——這一份一律用「覆蓋成新內容」）；不改檔名；不動 `js/` 的程式邏輯（`css` 只改註解）；不處理 §6 的待決；不動 `docs/SPEC_旅伴移除後的帳.md`。

## 4. 規則

- **R1 換日期**：把那三天換成另一組**合成的**連續三天。
  - 年、月、日**都換掉，不要同月同日**：git 歷史裡還查得到這個檔第一次 commit 的日期，同月同日會留下對得起來的線索。
  - 同一種格式、日曆上存在、連續三天、順序不變；**星期幾照舊**（原本是星期幾到星期幾，新的也一樣——有些畫面會自己算星期幾，保持一致最安全）。
  - 不選未來的日期（有些畫面分「還沒出發／已結束」）；**不跨月、不跨年，除非原本就跨**（原本跨不跨你看得到，照原樣）。
  - **用程式挑、不要手算**：寫一支暫存腳本找出符合上面條件的一組（腳本不進 repo）。
  - 行程檔、`shotxl.mjs`、`v148shots.mjs` 三處用**同一組**合成日期。
  - 行程檔裡的旅程名稱、景點、「家」都留著（定義 (a)；行程地點本來就不算）。
- **R2 測試**：改之前先跑一次 `itintest`、`importtest`、`plantest`，記下每一支的通過數；改完再跑，**通過數逐支相同、全綠，現有斷言一條都不改**。若有任何斷言非改不可，**停下來回報**——那代表統籌者的查證漏了什麼。其他受影響的照你的規則挑（`affectedtest` 會告訴你）。
- **R3 `css` 註解**：改成不帶日期與人數的說法（例如「『起訖日期 · N 天 · N 人』這種一整串」），技術理由原樣保留。只改註解；要不要 bump 照你改 SHELL 檔的規則。
- **R4 截圖——一律覆蓋、不刪、不改檔名**：
  - `_import/v1.38-03-paste-text.png`：R1 之後用 `importshots.mjs` 重產；`features/v1.38-貼上真實行程表.png` 用重產後的那張覆蓋（它本來就是複本）。
  - `_v148` 兩張海報：示範的是「前一張／下一張已藏」的箭頭，日期不是重點。可以用 R1 的日期重產（`v148shots` 會打真的 Nominatim，照它的使用規矩），或把抬頭的日期區塊**用不透明色塊蓋掉**（不要模糊，模糊有時還原得回來）。你選，回報理由。
  - `v1735` 修正前／修正後：這一組比的就是**日期列怎麼換行，日期是主角，不能遮**。用字數跟原本相同的合成日期、字數相同的合成行程名稱、合成的人數重產——字數一樣，換行行為才一樣。修正後用現在的程式；修正前用修正前那個 commit 的程式，在 repo 外的暫時 worktree 跑、跑完收掉（先拆 `node_modules` 的連結，不要 `--force`）。重產出來的一對要看得出原本那個換行問題；看不出來就停下來回報，不要交一對看不出差別的圖。
  - 每一張重產或遮蓋後的圖都**逐張打開看**，確認沒有真實日期、真實行程名稱配日期、真實人數。
- **R5 本機路徑**：`SETUP_TODO.md` 43 與兩支 `publish-cloud` 第 4 行的用法註解 → 「在本 repo 的根目錄」這類說法；`docs/STATUS.md` 1455 只改路徑那一段。**1455 同一行還有 §6 Q1 的群組 ID 前綴——這一份不動它**。
- **R6 STATUS 要記（不貼任何原本的值）**：清了哪幾類、各幾處；**這些在 git 歷史裡仍在**（沒改寫；GitHub 舊物件也撈得到）；§5 的覆蓋率界線；定義多了 §2 那幾條。

## 5. 覆蓋率的界線（照實寫進 STATUS，不要讓後人讀成「全部看過了」）

普查時 323 張圖**只逐張看了約 25 張**；其餘約 290 張是依「產生它們的腳本用的是合成資料」整批判定；三支用真實資料的腳本（`importshots`、`v148shots`、`shotxl`）產的圖，逐張看了可能出現日期的畫面。這一份清掉的是**看到的**；沒逐張看過的那一批，之後有需要再看。

## 6. 待決（Yolin 判定；這一份不動）

| # | 位置 | 是什麼 | 統籌者的建議 |
|---|---|---|---|
| Q1 | `CLAUDE.md` 35、`docs/STATUS.md` 80、1454 | 家人正在用的那組群組 ID 的**前 8 碼**（1454 行還寫了照片張數）| 換成不帶 ID 的說法（例如「那組家人群組」）。你推論前綴拿不來加入或讀取（Worker 每個端點都要完整 ID 加祕鑰、沒有用前綴查群組的路），合理但沒拿真實群組實測；換掉幾乎零成本 |
| Q2 | `js/nearby.js` 等多處 | 「找附近」的實測中心：台北某條路段與附近一所國小（半徑 1.5 公里）| 不管是不是住家附近，都換成公開的地標（例如車站）——幾乎零成本，而且 Yolin 不必說出住在哪裡 |
| Q3 | `scripts/shots1670.mjs` 22–23 | 寫死兩個本機 Claude 工作階段的 UUID（家目錄用環境變數）| 不算 device id（那是這台機器上工作階段的編號，不是裝置識別碼）；列清理建議，改讀環境變數 |
| Q4 | `docs/STATUS.md` 654、689 等 | 真實行程的統計數字（任務數、公里數），沒有日期也沒有身分 | 不算（定義 (d)）|
| Q5 | 邀請連結截圖（§0）| 真實的群組祕鑰可能曾入過圖、git 歷史裡仍在 | 先判斷是不是真的：解壓縮得出來的那一段裡如果有群組 ID，拿它的前 8 碼跟 Q1 那組真實群組的前 8 碼比——對不上就是模擬的，不必換祕鑰。對得上，或判斷不了 → 請 Yolin 決定要不要換一把祕鑰（換了家人要重新加入）|

## 7. 紀律與回報

- 共用慣例 v5 §5；若有新斷言，要證明會紅；含 regex 或反斜線的腳本用 Write／Edit 寫檔。
- **公開前自查**（email 對照組用合成樣本），並**用眼睛看一遍新增的每一行與每一張換掉的圖**——這一份的目的就是清個資，不能再帶進新的。
- 回報：R1 用什麼條件挑的、挑到的合成日期（**不要寫出原本的日期**）；R2 三支測試改動前後的通過數；每張圖怎麼處理（重產或遮蓋）與理由；STATUS 記了什麼；規格沒預期到的狀況。

## 8. 決策紀錄

| # | 誰 | 內容 |
|---|---|---|
| 1 | TripQuest 2026-09-23 | 普查回報：真實出遊日期 11 處、本機路徑 4 處、邀請連結截圖、四個定義問題、覆蓋率界線；建議整組平移到別的年份 |
| 2 | Dispatch 2026-09-23 | 先授權換掉邀請連結截圖（風險不對稱）；請統籌者寫清理規格、決定怎麼平移 |
| 3 | 統籌者 | 平移：年月日都換、連續三天與星期幾照舊、不選同月同日（歷史裡的 commit 日期會留下線索）、不選未來；圖一律覆蓋、不刪、不改名；`v1735` 那一組日期是主角，用字數相同的合成資料重產、不遮；四個定義問題的處理見 §2；§6 的五項等 Yolin |
