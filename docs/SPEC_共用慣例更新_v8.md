# SPEC_共用慣例更新_v8 — 副本更新到主檔 v8，並在閘門驗法補一種情境：自查範圍要照遠端的實際狀態算

> 狀態：**待執行——排在你手上現有的工作之後，不要插隊**
> 說明：由本 App 的開發 Session 執行。執行完把上一行改成「狀態：已執行（日期）」，然後把這一行說明整行刪掉。不用寫 commit hash。
> 撰寫：統籌 Session（Opus 5.5，effort Max），2026-09-23。JLPT_App、StockDiary、TripQuest 收到的是同一份；第 2 步的表只做你那一列。MealMate 已符合，收到的是只更新副本的另一份。
> 建議 effort：medium（副本，加上在閘門驗法裡補一種情境、做一次突變）。
> 不 bump 版本、不部署、不算一版（驗法腳本不在快取清單裡）。**不急**：副本落後不擋任何工作（共用慣例 §7.2）；轉交時機由 Dispatch 排。

## 為什麼改版

主檔 v7 → v8，四處，都是四個 App 做 v7 時挖出來的，每一條都有實證出處；Dispatch 2026-09-23 核可、不另問 Yolin（既有條文的延伸或措辭）。出處見附錄全文 §8 的 v8 那一行。

1. **§2.5 補「自查的範圍」與它的完整因果——這一份唯一要你改做法的一條。** 範圍要照遠端的實際狀態算，不能照本機的追蹤分支算。本機可能以為某個 commit 已經在遠端、遠端其實沒有：上一次「推送回報成功、遠端卻沒更新」之後就會這樣，而推送後那一關攔到的正是這種事。照本機的認定算範圍，那個 commit 會落在範圍外，下一次推送就不經檢查被帶出去。**你的閘門多半已經先抓遠端了，但驗法裡沒有一種情境專驗這一點**——「我本來就有做」跟「有東西擋著它被改掉」是兩回事：沒有這一種情境，下一個人把那一步拿掉，不會有任何測試紅。MealMate 找到這個洞時補了這一種情境；統籌者補上之後實測，拿掉那一步時，只有這一種報不符，其他情境全部照樣通過。
2. **§2.5 補「新增閘門時，盤點所有教人怎麼做這件事的地方」。** 你的 `CLAUDE.md` 在 v7 那一輪已經指向閘門，或本來就不教怎麼推；這一條不必再動。
3. **新增 §5.11「驗檢查器」三層**：是誰、為什麼擋的；擷取那個資訊的程式本身也要有對照組；驗法的對照組要真的作用在被驗的對象上。
4. **§0.5 區分「不適用」與「適用但尚未觸發」。**

## 要做的事

**1. 用下面附錄的全文整份覆寫 `docs/CONVENTIONS.md`。** 用 Write 工具；UTF-8、無 BOM、LF；第一行是版本標記。

**2. 你那一列（只做你那一列）。** 若你在收到這份之前已經補過第 3 步那一種情境，照 §0.5 的精神在 STATUS 寫明「已符合」、跳過那一步，並在回報裡說明補在哪裡。

| App | 要做的 | 已確認不必動 |
|---|---|---|
| JLPT_App | (1) 在 `scripts/test_pushsafe.sh` 補第 3 步的情境，並做第 3 步的突變。(2) **照 §5.11 第一層，既有情境補上比對理由**：目前除了「推送被拒」，其他情境都只比回傳值。「要推的檔有命中」「沒有要推的 commit」「抓不到遠端」都回 1，要從輸出分出是哪一個；造情境的 commit 沒做成就中止（v7 驗收時列過的建議）。 | 閘門先抓遠端、三關各有回傳值；`CLAUDE.md` 已指向閘門；§0.5 的「不適用」已寫 |
| StockDiary | 在 `scripts/gatetest.sh` 補第 3 步的情境，並做第 3 步的突變。你的閘門用 `FETCH_HEAD` 算範圍，本來就不看本機的追蹤分支；所以情境要用第 3 步「先推上假遠端、再把假遠端倒退」那種造法——只撥本機追蹤分支，對你的閘門造不出前提 | 驗法已比對是誰擋的、造情境失敗就中止、驗法自己也有對照組；`CLAUDE.md` 不教怎麼推；§0.5 的「適用但尚未觸發」已寫 |
| TripQuest | (1) 在 `scripts/pushgatetest.mjs` 補第 3 步的情境。你的推送閘用 `ls-remote` 問遠端的實際狀態，本來就不看本機的追蹤分支；突變是「把問遠端那一步換成讀本機的追蹤分支」。(2) **不擋、選做**：這一輪若會動到 `scripts/safe-push.sh`，順手把兩處 `ls-remote … \| cut -f1 \|\| true` 改成先存再取，`ls-remote` 失敗回 4、講明「抓不到遠端」（v1.74.9 驗收時列過的建議）；不動就留到下次 | 驗法已比對是誰擋的；`CLAUDE.md` 第 5 條已指向閘門；§0.5 的「不適用」與觸發條件已寫 |

**3. 情境：「本機以為已經推上去、遠端其實沒有」。** 在你驗法的暫存複本裡做（遠端＝暫存目錄裡的 bare repo，下面寫成 `$T/remote.git`），不碰 GitHub：

```bash
BASE=$(git --git-dir="$T/remote.git" rev-parse main)      # 假遠端目前的 main
# 1) 做一個帶命中的 commit（當場組出來的合成樣本），繞過閘門直接推上假遠端，再抓回來：
#    本機現在認定它已經在遠端（追蹤分支、FETCH_HEAD 都指著它）
git push -q origin main && git fetch -q origin main
# 2) 把假遠端的 main 退回 BASE：遠端其實沒有那個 commit
git --git-dir="$T/remote.git" update-ref refs/heads/main "$BASE"
# 3) 疊一個乾淨的 commit，跑你的推送那一整行
```

- **合格**：停在自查那一關，理由是抓到步驟 1 那個命中（照 §5.11 比對輸出裡是哪一關、哪一類）；假遠端仍是 `BASE`。
- **突變**：把閘門裡「取遠端實際狀態」那一步拿掉（或換成讀本機的追蹤分支），這一種情境必須**報不符**——帶命中的 commit 會被推到假遠端。突變要在暫存複本裡 commit 之後再驗，並確認驗到的真的是改壞的那一版（§5.11 第三層）；驗完丟掉複本。
- 造情境的每一步（commit、推上假遠端、倒退）失敗就中止，不要帶著沒造成的情境往下驗。

**4. STATUS 記一筆**：副本更新到 v8（日期）；第 2、3 步做了什麼；新情境與突變各自的回傳值與擋下理由。

## 不准做的事

- 只准動：`docs/CONVENTIONS.md`、本工單（新增，請一起 commit）、`docs/STATUS.md`、第 2 步你那一列提到的腳本。不改 `package.json`、`js/`、`sw.js`、`workers/`、既有測試的斷言。
- `CLAUDE.md` 這一輪不改。若你發現它還有教人繞過閘門的地方，寫進回報，由 Dispatch 個案授權。
- 會擋的情境只在假遠端上驗，不拿真的遠端試。
- 推送那一行不接管線。
- 不要動其他 App 與統籌工作區的檔案。條文若你覺得有問題：不要改副本，寫進回報。
- 不要自己更換本 Session 的模型或 effort。

## 驗證（結果貼進回報）

| # | 做什麼 | 合格條件 |
|---|---|---|
| U1 | `git status --short` | 只有：副本、本工單、`docs/STATUS.md`、第 2 步你那一列提到的腳本。多一個都要解釋 |
| U2 | 副本第一行 | 逐字等於 `<!-- CONVENTIONS v8 2026-09-23 -->`，無 BOM、無 CR |
| U3 | 副本與附錄全文比對 | 逐字相同；比對方法要有對照組（故意改一個字應判成不同） |
| U4 | `git diff -- docs/CONVENTIONS.md` | 改動只在：第一行、§0.5、§2.5（新增「自查的範圍」一段、改寫「盤點教人怎麼做的地方」那一句）、新增的 §5.11、§8 的 v8 那一行 |
| U5 | `git diff --stat -- CLAUDE.md` | 空的 |
| U6 | 第 3 步 | 新情境：回傳值、擋下理由、假遠端沒動。突變：這一種報不符、帶命中的 commit 到了假遠端、驗到的是改壞的那一版。JLPT_App 另附補上比對理由之後，每一種情境比對的是哪一句 |
| U7 | 推送 | 用你的閘門推送，不接管線；推完它自己比對遠端等於本機 |
| U8 | 本 App 會讀文件的既有稽核 | StockDiary：`npm run doctest`、`npm run shelltest`；TripQuest：`npm run audittest`；JLPT_App：無。全綠 |
| U9 | `git log -1 --format='%ae'` | 是 GitHub noreply（回報時不要印出任何真實信箱） |

## commit 與回報

- 副本與文件一個 commit，訊息以 `docs(conventions):` 開頭；驗法腳本另成一個 commit 也可以。
- 回報：**最前面（有回執就放在回執之後）寫一行你這個 Session 實際跑的模型與 effort**；然後 commit hash、U1–U9、第 2、3 步做了什麼、新的 §2.5、§5.11、§0.5 跟本 App 現行規定有沒有牴觸（請直說）、工單沒預期到的狀況。
- **之後全新開場的 Session，回執應該寫 `已讀共用慣例 v8（2026-09-23）`。**

---

## 附錄：副本全文（`docs/CONVENTIONS.md` 的內容＝下面區塊裡的文字）

````markdown
<!-- CONVENTIONS v8 2026-09-23 -->
# 四個 App 共用的工作慣例（CONVENTIONS.md）

適用：JLPT_App、StockDiary、TripQuest、MealMate。
主檔在統籌工作區（統籌 Session 的 repo）；每個 App 的 `docs/CONVENTIONS.md` 是內容相同的副本。**不要直接改副本**，要改照 §7。
這裡只收四個 App 都同意的規則。每個 App 自己的紅線、指令、門檻在它的 `CLAUDE.md` 與 `docs/STATUS.md`。

## §0 怎麼用這份

1. 開工前讀完本檔、本 App 的 `CLAUDE.md` 與 `docs/STATUS.md`。
2. 第一則回覆的第一行寫回執：`已讀共用慣例 vN（日期）`——N 與日期抄本檔第一行，不要抄這個範例。沒有回執，Yolin 就當你沒讀。
3. **本 App 的 `CLAUDE.md` 與 `docs/STATUS.md` 優先於本檔。** 兩邊衝突時照較嚴的那一邊做，並在回報裡指出來，讓統籌者修本檔。只有 §2 第 1、2、4、5 條（含第 4 條的細則附錄 A）與 §3 是專案不得放寬的。
4. 讀不到本檔：先試 `git show HEAD:docs/CONVENTIONS.md`；還是沒有，就在第一則回覆第一行寫「讀不到共用慣例」，然後照本 App 的 `CLAUDE.md` 與 `docs/STATUS.md` 工作。
5. **本檔的條文，本 App 有兩種情況要寫明在 STATUS**：**不適用**——寫哪一條、為什麼不適用、什麼情況下要回頭照做（例：沒有突變清單，所以 §5.9 不適用；將來建了突變清單再照做）；**適用但尚未觸發**——寫哪一條、要等什麼時候才照做、那時要做什麼（例：§5.10 要等加資料時才照做）。不要默默略過——默默略過的條文，下一個讀的人分不出是不適用還是忘了做。

## §1 角色與流程（Yolin 2026-09-14 明訂；模型與 effort 由 Yolin 2026-09-23 改定）

1. **規劃：統籌 Session，Opus 5.5，effort Max。** 理解需求、拆解、寫成規格。單一 App 的規格放該 App 的 `docs/SPEC_<主題>.md`；跨 App 的放統籌工作區。（2026-09-23 之前，統籌者是 Fable 5.1、effort high，開發 Session 是 Opus 5、effort high；那段期間的規格、投票與決定保留原樣，屬當時的模型設定。）
2. **開發：Opus 5.5，平常 effort medium；重大任務可上調至 high。** 開發 Session 負責實作，也負責查找、蒐集資料。統籌者在規格檔頭寫建議的 effort，建議 high 的附理由；實際的模型與 effort 由 Yolin／Dispatch 設定，Session 不自行更換。舊規定「只有 Yolin 明講大工程才切 Max」2026-09-23 起作廢。
3. **流程**：統籌者寫 spec → 開發 Session 實作＋查資料 → 回報並逐項與 spec 對照 → 與 Yolin 確認。
4. **較大改動或值得討論的議題**：由統籌者開 3 個代理投票，同一份 prompt、各自獨立、多數決、分歧採保守；各代理當統籌，查資料交給勘查代理；統籌者彙整後回報。**投票代理與統籌者同模型、同 effort**，三個整批在同一個模型與 effort 上跑，結果才可比；投票紀錄寫明實際的模型 ID 與 effort，工具指定不了 effort 時寫「繼承統籌者設定」。哪些算「較大」、實作途中臨時遇到時怎麼辦，看本 App 的規定。
5. **模型或額度不可用時，不要停下來問。**（「不要停下來問」是 Yolin 2026-09-10 明訂）子代理（投票、勘查）因額度或速率限制（429／quota）失敗 → **整批改用同一個可用的模型重跑**（投票不可混用模型）→ 回報最前面（回執之後）用一行註明實際用了哪個模型與 effort、原因。連一個可用的模型都沒有才暫停。統籌者與各 Session 本身用哪個模型與 effort 由 Yolin 設定，Session 不自行更換。

## §2 授權的地板

1. **這三件一定先問 Yolin**：刪除或破壞使用者資料；花錢、或需要註冊帳號／綁卡的服務；動到本 App 以外的任何檔案。本 App 的投票或其他決策流程**不能取代**這一條：就算投票通過，只要做下去有刪除或破壞使用者資料的風險（例如改儲存格式、做資料遷移），動手前仍然先問。
2. **其他 App 的目錄可以讀、不可以寫**（讀來抄慣例、對照同一種 bug）。統籌 Session 只會在本 App 寫 `docs/SPEC_<主題>.md`；它不改程式、不 commit、不 push。
3. **本檔不規定的**：一般 commit／push／部署、改寫 git 歷史、force push、刪除已 commit 的檔案、建新 repo——看本 App 的 `CLAUDE.md` 與 `docs/STATUS.md`。那裡也沒寫的破壞性操作，一律當作要先問。
4. **金鑰與個資不進 repo。**（Yolin 2026-09-19：「只要不會有金鑰或個資上傳到公開就沒有問題」；四個 repo 都是 public，推上去就收不回來）要 commit 的內容不得出現：金鑰或 token、真實 email、本機使用者名稱、本機絕對路徑（用 `~`、`$HOME` 寫的泛稱不算），以及 Yolin 與家人**真實的**個人資料——例如持股與金額、帳戶與交易、備份檔的內容、行程的具體日期、家人的名字與年齡。**什麼算、什麼不算，照附錄 A**（2026-09-23 定案；行程地點、飲食限制、病名與診斷都不算）。規格或 Dispatch 要你拿真實資料驗證時，**寫進 repo 的只能是結論，或換成合成的數字**；原始資料不進 repo。自查腳本抓得到前四類、抓不到個資——push 前自己把新增的文件行看一遍。拿不準算不算，就當成是，在回報裡提出來。
5. **推送的閘門：每一關都要真的擋得下。**（Yolin 2026-09-19 那句「只要不會有金鑰或個資上傳到公開就沒有問題」的執行方式）推送流程裡每一個「失敗就該停」的步驟，失敗時都必須回傳非 0，而且真的讓後面的步驟停下來：
   - **自查**：有命中、或任何一類的對照組沒命中，都回傳非 0，不推。
   - **推送本身**：失敗就停。不要接著做線上確認或整套檢查——那會對著沒推上去的舊版去驗，看起來還是綠的。
   - **推送後**：比對遠端的 HEAD 等於本機，才算推上去；這一道堵住「推了但沒成功」。

   **自查的範圍，是這次要推的每一個 commit 會公開的東西**：逐個 commit 的新增行（不要只比兩端——中間某個 commit 加了又刪掉的內容，照樣會被推上去；中途刪掉的檔也一樣）、commit 訊息、作者與提交者的名字與信箱。**範圍要照遠端的實際狀態算，不能照本機的追蹤分支算**：推送前先 `fetch`（或直接問遠端），讀不到遠端就停在自查那一關。原因：本機的追蹤分支可能跑在遠端前面——例如上一次「推送回報成功、遠端卻沒更新」之後（推送後那一關攔到的正是這種事），或遠端被倒退過；那時本機以為已經推上去的 commit，遠端其實沒有，照追蹤分支算，它們會落在範圍外，下一次推送就不經檢查被帶出去。**驗法要有一種情境專驗這點**：讓本機以為一個帶命中的 commit 已經在遠端、遠端其實沒有（例如先把它推上測試遠端、再把測試遠端的 main 退回舊值，或直接把本機的追蹤分支撥到它），再疊一個乾淨的 commit，閘門必須照樣擋下；把「取遠端實際狀態」那一步拿掉時，這一種必須報不符。**「我本來就有做」跟「有東西擋著它被改掉」是兩回事**：程式寫對了、註解也寫了理由，沒有這一種情境，下一個人把那一步拿掉，也不會有任何測試紅。

   **不要用管線包任何一步**（例如為了截短輸出加 `| tail -1`）：管線的回傳值是最後一個指令的，前面那一步的失敗會被吞掉，`&&` 照樣往下走，不報錯、沒有任何徵兆，只會在真的該擋的那一次默默放行。要看輸出就導到檔案再看，或在同一個 shell 先 `set -o pipefail`。

   **要用實際推送的那一整行實測，而且分別製造每一關的失敗**，不能只驗第一關：自查——餵一個一定會命中的合成樣本（放在自查會掃到的地方）；推送——讓推送失敗；推送後——讓遠端與本機對不上。製造失敗而不碰 GitHub 的做法：在暫存目錄建一個 bare repo 當測試遠端，放一個回傳 1 的 `pre-receive` hook 就是「推送被拒」，放一個把 main 退回舊值的 `post-receive` hook 就是「推送回報成功、遠端卻沒更新」。會擋的情境用 `--dry-run` 或這種本機測試遠端當保險，就算閘門沒擋住也只是試推、不會公開；用過真的遠端當測試，測試分支驗完就刪，再用 `git ls-remote` 確認遠端從來沒出現過它。每一關失敗用不同的回傳值（例如自查 1、推送 2、遠端與本機對不上 3），一看就知道是哪一關。

   **閘門腳本放在 repo 裡，不放 session 的暫存區。** session 會被刪、會撞額度、會被換掉；閘門跟著消失，就有一段沒有防線的空窗，新 session 重建時還可能重犯同樣的錯。**規則進 repo、資料不進 repo**：不能公開的內容（例如個資黑名單）從被 `.gitignore` 擋掉的檔案讀進來，並在 STATUS 寫明新 session 怎麼取得那個檔——否則搬進 repo 之後，新 session 照樣卡住。**新增一道閘門（或任何「以後都要這樣做」的規則）時，同時盤點所有教人怎麼做這件事的地方**——`CLAUDE.md`、README、STATUS 裡的每版流程、規格與工單範本——一起改成指向閘門；只改規則、不改這些地方，規則生效了、入口還開著，新 session 照著 `CLAUDE.md` 做就繞過閘門。改過閘門就把「分別製造每一關的失敗」那組驗法重跑一次；驗法最好也寫成 repo 裡的腳本。

   （2026-09-23 查了四個 Session 的推送那一行，沒有一個是乾淨的：三個在自查後面接了 `| tail -1`，自查失敗被吞掉，其中一次一個 commit 帶著自查命中被推上去；另一個的自查是好的，但在推送後面接了 `| tail -1`，推送失敗時，後面的線上確認照樣對著舊版跑。幾次都剛好沒有個資。）

## §3 溝通

1. **禁止互動式提示框／多選題工具（`AskUserQuestion` 之類）。**（Yolin 2026-09-16 明訂）需要 Yolin 決定的事一律用**純文字**寫在回覆裡：列出選項、每個選項的代價、你的建議與理由，然後**停下來等**，由 Dispatch 轉達。理由：Yolin 常從手機或另一台電腦遠端操作，提示框只渲染在本機那台 PC，遠端點不到，Session 就此卡死。
2. **全程繁體中文，包括思考與判斷的過程。**（Yolin 2026-09-16 明訂）維持原樣不硬翻：程式碼、變數與函式名、檔名與路徑、指令、專有名詞、引用出處。理由：Yolin 要能看懂你為什麼這樣決定。
3. **回報分開寫「實測驗證過的」與「推論的」。** 結尾放需要 Yolin 決定的事項，沒有就明講沒有。

## §4 給使用者看的文案要誠實

拿不到、算不出、還沒開放的，就照實寫（「尚未取得」「未估算」「這一版尚未開放」這類話）——不顯示 0、不用預設值冒充使用者輸入、不用舊資料冒充新資料。

## §5 測試紀律

1. **每條新斷言都要證明它會紅**：把對應邏輯改壞（突變），或拿修正前的程式跑，測試必須失敗；修回去必須綠。修 bug 時補一條「修正前會紅」的回歸斷言，並說明既有測試為什麼沒抓到。有新行為就補斷言。
2. **不寫假斷言。** 抓到過的形狀：母體是空的；母體恰好是「有問題的那幾個」；等待條件在上一頁就已經成立；反例太弱、被別條規則順便擋掉；掃描器把註解當程式碼；「應該是 0」旁邊沒有「母體非空」。寫完一條 `noneOf`／`everyOf` 就問一次：**這個母體現在有幾個元素？** 註解不是斷言。
3. **檢查器要有對照組**：餵已知該被抓到的輸入，斷言真的抓到。**斷言驗語意、不貼字面。** 對照組**一定要有**自己造的合成樣本，而且跑的是同一個檢查（同一個搜尋式、同一段程式）；真實資料可以當第二道對照，不能單獨當對照組——它會隨時間變（實例：公開前自查的 email 對照組用 git 歷史的作者信箱，改用 noreply 之後就被排除規則濾掉、命中 0；路徑類的對照組去搜本 App 的 `CLAUDE.md`，那些路徑清掉之後，目標與對照組都是 0 命中，自查看起來正常、其實早就失效；「預期只命中本檔這一行」這種寫法，本檔那一行只碰得到其中幾個分支，其餘分支壞了也看不出來）。**每一類、每一個分支都要有自己的合成樣本。**
4. **從使用者實際會走的入口測**：每個功能至少一條斷言從真實入口（畫面上的按鈕、對外的函式）進去，不能只驗內層純函式。
5. **含 regex 或反斜線的測試碼、補丁，一律用 Write／Edit 工具直接寫檔，不要經過 shell heredoc 或 `node -e`**：`\s` 變 `\\s` 或被吃掉都不報錯、測試照樣綠、但永遠不命中。
6. **打真網路的測試不進 `npm test`**，另開獨立指令；對外部服務的請求頻率照該服務的規矩。
7. **不必每一版都跑全套測試：平常跑受影響的就夠，全面檢測由 Yolin 定期指定。**（Yolin 2026-09-12 對全部專案的指示；2026-09-19 重申：「不用每一次小改動都全測，我會定期進行全面檢測」）這一條是**解除義務，不是設上限**——想多跑、想跑全套，永遠可以，不需要誰同意；本 App 規定每版要跑哪些的，照本 App 的規定。怎麼判定「受影響」由各 App 自己定。沒有放寬的那一條：新斷言仍然必須證明會紅。
   **耗時很久的整套（突變整套、全面檢測）**：等一批交辦事項做完再一起跑、一起看（Yolin 2026-09-19），不必每版跑、也不照固定天數。由 Yolin 指定；Session 不必自己跑（想跑永遠可以）。各 App 在自己的 STATUS 寫明「整套」指什麼、實測要跑多久、以及上限（距上次整套幾版、幾條突變或新斷言從未整套跑過）——上限照本 App 整套的成本訂：要跑幾小時的訂緊一點；只要幾分鐘的可以訂得很寬，或乾脆寫「每版都跑整套、不需要上限」。耗時還沒量過就寫「待量」，不要猜；超過上限時，回報最前面用一行講出來並建議「這一批做完就跑」，沒超過不必每次提。**成本算對地方**：整套是機器在背景跑，花的是時間，不是 token；花 token 的是處理結果的來回——所以要省的是「處理的次數」，結果一次回報、依根因分組。回報寫明是在哪個 commit 上跑的；沒紅的先在最新版複驗再處理。**整套的證據要進 repo**（Yolin 2026-09-23）：每次跑完，把日期、commit、範圍、條數、結果、耗時、沒紅的清單寫進 STATUS 或一個進版控的摘要檔——只存在本機 log 等於沒有紀錄。**完整的 log 寫到 repo 底下、被 `.gitignore` 擋掉的目錄（例如 `.logs/`）**，檔名帶日期與 commit；不要寫在系統暫存目錄或 session 的暫存區——跑好幾個小時的整套，啟動它的 session 可能中途被關掉，暫存目錄會跟著消失。**刪一條突變之前，要能說出這段程式為什麼在任何真實路徑上都走不到**；說不出來，就是測試走錯路，要改的是測試。
8. 隨機或情境敏感的輸出（排程、排菜、抽樣）不得綁死單一結果。需要特定情境才分得出對錯的斷言：優先自己造情境、直接呼叫被測的函式；若用整段產生的結果，先寫一條前置斷言確認情境存在，不成立就紅。**固定 seed 不算釘住情境**（資料或規則一改，同一個 seed 的結果就變）；沒有前置的「至少 1 個」就是假斷言。前置斷言自己也要用突變證明會紅（把情境拿掉，它必須紅）。統計型斷言另照各 App 的做法（多組種子量餘裕）。沒有單元測試層、只有端對端測試的 App，「自己造情境」就用它造得出來的方式（預置儲存狀態、URL 參數）；前置斷言照樣要。
9. **突變的 `expect`**（有這個機制的 App）：`expect` 是用來確認「紅的是指定的那一條」，所以**不要寫會變動的數字或整句訊息**——斷言訊息一改，突變就變成「紅錯地方」。訊息本身沒有穩定的開頭時，不要把 `expect` 寫鬆，而是**在訊息前面加一個固定標籤當錨點**（例：訊息寫成「早餐道數門檻：早餐 N 道…」，`expect` 比「早餐道數門檻：」）。改任何斷言的訊息時，一起搜突變清單裡含舊文字的 `expect`。秒級的突變檢查要同時查「`expect` 在對應測試的原始碼裡找得到」，不必等整套才發現過期；還沒有這個檢查的，列進本 App 的待辦，下次動突變機制時補。
10. **加資料之前，先查畫面上依筆數寫死的上限**（例如選單只列前 N 筆）：新加的資料可能永遠排不進畫面，卻沒有任何測試會紅。有這種上限的，加完要有一條斷言從畫面數得到新資料。
11. **驗一個檢查器或閘門時，光確認「它擋下來了」不夠**，分三層，缺一層就可能是假驗證：
    - **是誰、為什麼擋的**：預期失敗的情境，要比對失敗的內容與位置（輸出裡哪一關、哪一支、哪一類、哪個檔），不能只比對回傳值——同一個回傳值可能是別的原因造成的，甚至是驗法根本沒跑。造情境的步驟（例如測試用的 commit）失敗就中止，不要帶著沒造成的情境往下驗。
    - **擷取那個資訊的程式本身也要有對照組**：它若抓空，「比對是誰擋的」就默默退化成只剩回傳值。拿一段已知的輸出餵給它，確認抓得到該抓的那一句。
    - **驗法的對照組要確認真的作用在被驗的對象上**：把被驗的東西改壞一處，驗法必須報不符；而且要確認改壞的那一份真的被執行到——複本、分支、路徑對錯了，壞的那一份根本沒跑，驗法照樣全部「符合」。

    （2026-09-23 的實例：一個 App 的驗法有一種情境回傳 1，擋下它的卻是另一類檢查——造情境的 commit 沒做成，合成字面又被另一類掃到；另一個 App 有去比對「是誰擋的」，但擷取擋下原因的樣式沒對到巢狀括號，前兩種情境抓空、只剩回傳值；第三個 App 的三種壞閘門都「回傳 1」，卻沒有任何一種情境報不符——驗法複製過去推的是 `main`，不是當時的 detached HEAD，壞閘門根本沒被執行到。）

## §6 文件

1. **`docs/STATUS.md` 是接手契約。** 最前面要有「目前進行中／交接」與「等 Yolin 回覆」這兩塊（叫什麼名字、是獨立一節還是小節，照本 App 現有的寫法）；這兩塊過期了就改寫，不要往下追加。只存在對話裡的決定、規則、踩坑，收尾前寫進 STATUS。
2. 規格 `docs/SPEC_<主題>.md`；投票紀錄 `docs/VOTE_<日期>.md`。
3. 文件裡的數字盡量由靜態稽核從程式數出來比對；文件會漂移，而漂移不會讓任何測試變紅。

## §7 本檔怎麼改

1. **一條規則該進本檔還是留專案**，依序問：(a) 把規則裡的 App 名、檔名、數字換成另一個 App 的，還成立且措辭不必改嗎？(b) 在任何一個 App 違反它，Yolin 都會認為做錯了嗎？(c) 它是「原則＋某 App 的執行細節」嗎？——(a)(b) 都是 → 原則進本檔，細節留專案。領域紅線（健康、投資、使用者資料）永遠留專案；Yolin 指名「全部專案」的直接進本檔。
2. **只有統籌 Session 改主檔**：第一行版本 +1、日期改當天、§8 加一行。各 App 的副本由該 App 自己的 Session 依統籌者寫的工單更新。副本落後不擋任何工作。
3. 開發 Session 發現值得變成通則的規則：先照常寫進本 App 的 STATUS，並在回報裡標「候選通則」，由 Dispatch 轉給統籌者。不要改副本。
4. Yolin 明訂的直接進；Session 提議的要 Yolin 點頭；會改變四個 App 做法的才開 3 代理投票。

## §8 變更紀錄

- v8（2026-09-23）：四處，都是四個 App 做 v7 時挖出來的，每一條都有實證出處；Dispatch 2026-09-23 核可、不另問 Yolin（既有條文的延伸或措辭）。**§2.5 補「自查的範圍」**：逐個 commit 的新增行、commit 訊息、作者信箱；範圍照遠端的實際狀態算——本機的追蹤分支可能跑在遠端前面（推送後那一關攔到的事故正是前提），並要有專驗這點的情境，拿掉「取遠端實際狀態」那一步時必須報不符。**§2.5 補「新增閘門時，盤點所有教人怎麼做這件事的地方」**（v7 時三個 App 的 `CLAUDE.md` 都寫著直接推送）。**新增 §5.11「驗檢查器」三層**：是誰為什麼擋、擷取那個資訊的程式也要有對照組、驗法的對照組要真的作用在被驗的對象上（三個 App 各踩到一層）。**§0.5 區分「不適用」與「適用但尚未觸發」**。
- v7（2026-09-23）：五處。**新增 §2.5「推送的閘門」並列入 §0.3 不得放寬**：推送流程的每一關（自查、推送本身、推送後比對遠端）失敗都要真的讓後面停下；不用管線包任何一步；用實際推送的那一整行、分別製造每一關的失敗來驗（附不碰 GitHub 的做法）；閘門腳本放 repo、不放 session 暫存區，規則進 repo、資料不進 repo。起因：2026-09-23 查了四個 Session 的推送那一行，沒有一個是乾淨的。這是 Yolin 自己定的 §2.4 的執行方式，Dispatch 同意不另問；「會擋的情境用 `--dry-run` 或測試遠端當保險」「每一關不同回傳值、最後比對遠端」兩個設計來自 App。**新增 §0.5「不適用的條文要寫明理由與觸發條件」**（一個 App 做 v6 副本更新時的寫法；Yolin 2026-09-23 核准）。**附錄 A 第 1 點補「地點的組合」**：單獨一個公開地標不算，好幾個地點合起來指向同一區才算（Yolin 2026-09-23 核准）。**§5.3 補兩個實例**（措辭，原則 v6 已寫）。**補記**：v6 的 §5.7 後半、§5.9、§5.10 三條是 Session 的做法，Yolin 2026-09-23 已同意（放工單之後才同意，當時 v6 不改）。
- v6（2026-09-23）：六處。**§2.4 與新增的附錄 A「個資怎麼判定」**：TripQuest、MealMate、JLPT_App 做了既有檔案的個資普查，v5 的定義有好幾處不好判（本機路徑沒分級、使用紀錄沒說是誰的、公開 commit 作者欄、像憑證卻解不開的字串）；Yolin 2026-09-23 判了三個邊界（行程地點、飲食限制、病名與診斷都不算）與幾個清理問題，定義由 Dispatch 起草、統籌者依三份回報補齊。v5 §2.4 的例子「行程的日期與地點」「家人的健康或飲食狀況」跟這些判定衝突，一併改掉；附錄 A 同屬 §0.3 不得放寬。**§5.7 補「整套的證據要進 repo、完整的 log 放 `.logs/`」**：前半 Yolin 2026-09-23 核准（MealMate 2026-09-21 的整套結果差點只剩在系統暫存目錄裡）；後半是 StockDiary 的做法，Dispatch 指定列入。**§5.3 寫清楚**：合成樣本是必要的那一道、跑的是同一個檢查，真實資料只能當第二道（措辭，原意不變）。**新增 §5.9 突變的 `expect`**：MealMate 2026-09-21 整套沒紅的三條裡，兩條是斷言訊息改了、`expect` 沒跟上；固定標籤的做法來自 MealMate，秒級檢查查 `expect` 來自 StockDiary；Dispatch 指定列入。**新增 §5.10 加資料前查寫死的上限**（2026-09-21 替 MealMate 加菜時漏看了畫面上的筆數上限；Dispatch 指定列入）。「規格要你拿真實資料驗證時，寫進 repo 的只能是結論」v5 §2.4 已經有，不重複。沒有開投票：§2.4 與附錄 A 是 Yolin 的判定；§5.3 是措辭；§5.7 後半、§5.9、§5.10 是 Session 的做法，由 Dispatch 指定列入。
- v5（2026-09-23）：兩處，都是 Yolin 親口定的，沒有開投票。**§1 改成 Yolin 2026-09-23 的模型政策**，原話：「之後統籌者為 Opus 5.5 + effort Max，其餘 Session 為 Opus 5.5 + effort medium，若有重大任務可上調至 high」——統籌者 Opus 5.5／Max；開發 Session Opus 5.5／medium，重大任務可上調 high（統籌者在規格檔頭建議、Yolin／Dispatch 設定）；投票代理與統籌者同模型同 effort、紀錄寫明模型 ID 與 effort；舊的「Fable 額度用盡改用 Opus 5」前提（統籌者是 Fable）已不成立，改寫成不綁特定模型的「整批改用同一個可用的模型、不要停下來問」，它帶著的兩個原則（不要停下來問、投票整批同模型）保留。**§2.4 新增「金鑰與個資不進 repo」並列入 §0.3 不得放寬**：Yolin 2026-09-19 就說過（「只要不會有金鑰或個資上傳到公開就沒有問題」），主檔一直沒寫；2026-09-21 StockDiary 照規格拿真實資料驗證、把結果照抄進 STATUS，個人財務資料差點推上 public repo（push 前的驗收攔下）。開頭說明裡的「Fable 統籌 Session」改成「統籌 Session」。
- v4（2026-09-21）：四處，都來自四個 App 做 v3 更新時的回報，沒有新政策。§0.2 回執範例改成不含版本的寫法（v3 升版時統籌者漏改範例，TripQuest、StockDiary、JLPT_App 各自抓到）。§5.3 補「對照組用合成樣本」（StockDiary：改用 noreply 後，拿作者信箱當自查對照組會失效）。§5.7 補「寫明整套是什麼、實測多久；上限照成本訂；沒量過寫待量」（四個 App 的整套從 222 秒到 4.1 小時，差三個數量級；v3 是照最貴的那個寫的）。§5.8 替只有端對端測試的 App 留口（JLPT_App）。
- v3（2026-09-21）：§5.7 補「耗時很久的整套」一段、§5.8 改寫。起因：Yolin 2026-09-19 問「耗時很久的突變不用這麼頻繁做，等手邊的待改事項完成後再一起檢視」；同一天 MealMate 全面檢測抓到 3 條「情境消失、斷言照樣綠」——§5.8 原文早就要求前置斷言，但沒講「固定 seed 不算釘住情境」。3 代理投票六題皆 3:0（§5.8 改寫但要短；整套的時機進共用、上限數字留各 App；操作細節留各 App，只進「刪突變前先確認真實路徑」與「成本寫明」）。代理補的三點已納入：前置斷言也要突變驗紅、「Session 不自己跑」與「解除義務不是設上限」矛盾故改成「不必」、回報寫明 commit 並在最新版複驗。
- v2（2026-09-19）：三處措辭修正，都來自四個 App 導入時的回報。§5.7 改寫——原文讀起來像在限制跑全面檢測，JLPT_App 與 MealMate 都因此誤讀或列為矛盾；現在明講是解除義務、不是設上限。§2.1 補一句：投票不能取代「刪除或破壞使用者資料先問」（JLPT_App 回報它的投票制與這一條有交集；統籌者決定地板優先，Yolin 可以否決）。§6.1 那兩塊的名稱與層級照各 App（StockDiary、JLPT_App 回報結構不同）。
- v1（2026-09-19）：初版。只收四個 App 現有條文的交集。規格：統籌工作區的 `SPEC_共用慣例.md`（v2）。

## 附錄 A 個資怎麼判定（§2.4 的細則，同屬不得放寬；2026-09-23 定案）

只算**真實的、連得到 Yolin 或他家人的**資料；自己造的測試資料、標明是合成的範例、講產品情境的敘述（「有長輩的家庭」）都不算。判斷的精神（Yolin 2026-09-23）：**「去過哪裡」「吃什麼、不吃什麼」「有什麼病」不算；「誰、在哪一天、在哪裡」、錢、身分、憑證才算。** 拿不準就當成算，在回報裡提出來。

1. **算個資**（不得寫進 repo；已經在 repo 裡的要回報）：
   - **財務**：金額、股數、持有的標的代號、扣款筆數、投資年數、總市值、報酬率、真實的花費紀錄。單獨出現的公開代號不算，跟「Yolin 持有」「他的計畫」連在一起才算。
   - **住家與「哪一天在哪裡」**：住家、工作地點、學校，不需要日期就算；功能實測或示範用的真實位置，拿不準是不是住家附近，就當成住家、換成公開地標。單獨一個公開地標（車站、醫院、商場）不算；好幾個地點合起來指向同一個小範圍（例如同一次功能實測裡出現的路段、學校、醫院、停車場），才當成可能的住家附近處理——拆掉那個組合，換成公開地標或泛稱；同一個名稱在測試與文件裡要一起改。真實成行（過去或未來）的**日期＋地點**，日期要具體到哪一天，只到月份或年份的不算；未來的行程＋具體日期最優先（那段時間家裡沒人）。真實行程的名稱＋真實日期也算；家人同行的統計數字（公里、任務、照片、人數）跟真實日期或行程名稱連在一起時也算。
   - **身分**：Yolin 與家人的真實姓名、年齡；學校、系所、工作單位。「Yolin」這個代稱、GitHub 帳號、阿公阿嬤這類稱呼不算。
   - **識別資訊**：內網 IP、MAC、device id、帳號名稱、真實 email（GitHub 與 Anthropic 的 noreply 除外）、本機使用者名稱、帶著使用者名稱的本機路徑（包括展開後的家目錄）。
   - **使用紀錄**：Yolin 或家人**真實**紀錄裡推得出作息或程度的具體數字（作答數、連續天數、程度、固定哪幾天不在家吃）。開發或測試時自己跑出來的數字不算。
   - **憑證（最優先）**：金鑰、token、邀請祕鑰或邀請碼（含畫面上的 QR code）、真實的 group／trip ID——只要拿得來加入或讀取真實資料，不論旁邊有沒有地名。看起來像憑證、卻解不開的字串，不能證明是假的就當真的。發現了先回報這一項，不貼值、不自己換或刪（換了家人會連不上）。
2. **不算個資，但也不要新寫進 repo**（已經在 repo 裡的，列成「清理建議」回報，清不清另外決定）：
   - 只有磁碟機代號加專案的本機路徑；編譯產物、壓縮檔裡帶的路徑（例如 `__pycache__` 裡的 `.pyc`）。
   - ID 的前綴：拿不來加入或讀取，但對得到那一組家人——換成不帶 ID 的說法。
   - 本機工作階段的編號（例如 session UUID，它不是 device id）——改讀環境變數。
   - 從 repo 移除已 commit 的檔屬於刪除已 commit 的檔：只寫建議，由 Yolin 決定。
3. **不算**（不必回報、也不必清）：
   - 行程地點（地名、景點、店名、住宿），只要沒有具體日期、也沒有真實 ID 連著；飲食限制與實際排過的菜單；病名與診斷、身心狀況、過敏。
   - 用 `~`、`$HOME`、`%USERPROFILE%` 寫的泛稱路徑；每台電腦都一樣的系統路徑。
   - 公開 commit 作者欄的帳號名稱（含改名前的舊名）。
   - 一般化的敘述（「家裡有長輩」「明年要出國」）、家庭組成、裝置型號與系統版本、沒有日期也沒有身分的行程統計。
   - 標明合成的範例、自己造的測試資料與人名；公開的市場或規格事實；第三方文獻作者、官方機構或專案的公開信箱。
   - 開發過程的紀錄（「某天回覆」「整套跑了多久」）：文件本身的日期不是成行日期。
4. **已經在 git 歷史裡的**：不在普查範圍，也不必回報——包括 2026-09-19 改用 noreply 之前、公開 commit 作者欄裡的真實 email。Yolin 2026-09-17 決定不重寫 git 歷史，要改寫只有他能決定。從最新版清掉的，在 STATUS 記一句「歷史裡仍查得到」。
5. **可能外洩、卻無法證實的憑證**：先從最新版換掉看得到的部分。要不要輪替由 Yolin 決定；輪替的代價高於風險時，他可以選擇不輪替。這種決定寫進本 App 的 STATUS：理由、已知並接受的風險、什麼情況要重新檢視（例如出現異常存取的跡象）。
6. **查的時候**：列檔用 `git -c core.quotepath=false`（否則中文檔名會被加引號、副檔名篩不到）；每一類搜尋式先在合成樣本上命中；二進位檔的命中看落在哪裡——PNG 只有文字類區塊（`tEXt`、`iTXt`、`zTXt`、`eXIf`）才會帶路徑或中繼資料，壓縮影像資料裡湊出來的短樣式是巧合；JPEG 看 EXIF。**查到的結果（哪一行有個資）不寫進 repo**：那份清單本身就是地圖，回報只寫檔名、行號、類別。
````
