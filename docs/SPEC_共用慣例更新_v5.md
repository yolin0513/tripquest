# SPEC_共用慣例更新_v5 — 副本更新到主檔 v5；本 App 文件裡的舊模型設定跟著改

> 狀態：已執行（2026-09-23）
> 撰寫：統籌 Session（Opus 5.5，effort Max），2026-09-23。四個 App 收到的是同一份工單；第 2、3 步的表只做你那一列。
> 建議 effort：medium（docs-only，沒有程式與測試的改動）。
> docs-only：不 bump 版本、不部署、不算一版。**比平常急一點，請排在你下一個工作的最前面**：你的 `CLAUDE.md`／`docs/STATUS.md` 還寫著舊的模型設定，而依共用慣例 §0.3，本 App 的檔優先於共用慣例——不改的話，新規則會被舊字面蓋過。

## 為什麼改版

主檔 v4 → v5，兩處，**都是 Yolin 親口定的**，所以沒有開投票：

1. **§1 模型與 effort（Yolin 2026-09-23）**。原話：「之後統籌者為 Opus 5.5 + effort Max，其餘 Session 為 Opus 5.5 + effort medium，若有重大任務可上調至 high」。
   - 統籌者：Fable 5.1（effort high）→ **Opus 5.5，effort Max**。
   - 開發 Session（就是你）：Opus 5（平常 high、大工程才切 Max）→ **Opus 5.5，平常 medium；重大任務可上調至 high**。統籌者會在規格檔頭寫「建議 effort」；實際的模型與 effort 由 Yolin／Dispatch 設定，**Session 不自行更換**。「大工程才切 Max」作廢。
   - 三代理投票：代理與統籌者**同模型、同 effort**，三個整批在同一個模型與 effort 上跑；紀錄寫明實際的模型 ID 與 effort（工具指定不了 effort 就寫「繼承統籌者設定」）。
   - 「Fable 額度用盡就改用 `claude-opus-5`」**改寫成不綁模型的版本**：子代理因額度或速率限制失敗 → 整批改用同一個可用的模型重跑、不要停下來問、回報註明用了哪個模型與 effort、原因。舊條的前提（統籌者是 Fable）已經不成立；它帶著的兩個原則——不要停下來問（Yolin 2026-09-10）、投票整批同模型——保留。
2. **§2.4 新增「金鑰與個資不進 repo」**，並列進 §0.3「專案不得放寬」的條文。Yolin 2026-09-19 就說過：「只要不會有金鑰或個資上傳到公開就沒有問題」，但主檔一直沒寫。2026-09-21 StockDiary 照規格拿真實資料驗證，把結果照抄進 STATUS，個人財務資料差點推上 public repo（push 前的驗收攔下，改寫了那個還沒推的 commit）。新條文的重點：真實資料驗證的結果，寫進 repo 的只能是結論或合成數字；自查腳本抓不到個資，push 前自己看一遍新增的文件行。**這一輪不用回頭盤點既有文件**（要不要盤點另外決定）；從這個 commit 起照做。

## 要做的事

**1. 用下面附錄的全文整份覆寫 `docs/CONVENTIONS.md`。** 用 Write 工具；UTF-8、無 BOM、LF；第一行是版本標記。掛得到統籌工作區的話，直接複製它根目錄的 `CONVENTIONS.md` 也可以，兩者必須相同。

**2. 本 App 自己文件裡「現行的」模型設定，改成跟新 §1 一致。**（這一輪的重點）
原則是**規範要更新，歷史要保真**：
- **要改**：描述「現在怎麼做」的條文——開發流程慣例（規劃／開發／流程／投票四條）、工作慣例裡「開 3 個 Fable 5.1 代理」「Fable 額度用盡改用 Opus 5」、`CLAUDE.md` 裡講模型或投票主持者的句子、待決問題與草稿裡描述流程的句子。
- **不改**：有日期的紀錄與事後說明（「v1.66 那輪改用 Opus 5 執行」「09-14 起由 Fable 主持」這類）、已執行規格檔頭的「撰寫：Fable…」、投票紀錄 `VOTE_*.md` 的模型欄、`PLAN.md` 開頭的「規劃：Fable 5.1。開發：Opus 5。」——這些是當時的事實。必要時在句尾加「（當時的模型設定）」，不要改寫。
- **寫法**：照新 §1 的意思改寫，不要把整段 §1 抄一份進來；能寫成「照共用慣例 §1」的就指過去。
- **`CLAUDE.md` 是例外授權**：只准改下表與第 3 步表列出的那幾行（行號是統籌者 2026-09-23 讀到的，以你讀到的為準），其餘一字不動，尤其是檔尾的「共用慣例」指向段。

| App | 統籌者 2026-09-23 查到的現行條文（要改） | 已確認是歷史或無關（不改） |
|---|---|---|
| JLPT_App | `CLAUDE.md` 第 17 行「先走多代理投票（由 Fable 主持）」→ 由統籌者主持。STATUS：§3「開發流程慣例」第 1–4 條（約 181–184 行）；約 79 行「其餘 SPEC 依流程由 Fable 評估」；§2 Q3（約 116–118 行）與 Q9（約 156 行）的敘述——**Q3 是等 Yolin 的待決問題，只改字面（Fable → 統籌者、「opus 代理」→「代理」），題意與建議不變，不要替 Yolin 回答**；§3.3「主持者」那一行（約 260 行）與「已失效的舊規則」表那一列（約 296 行）改成「由統籌者主持」、保留日期。`docs/SPEC_feature_candidates.md` 第 6 行「由 Fable 評估」 | 已執行的規格檔頭；STATUS 裡「大工程：讀解題庫」是在講工作量，不是 effort 規則 |
| StockDiary | STATUS「工作慣例」第 2 條（開 3 個 Fable 5.1 代理）與第 3 條（Fable 額度用盡），約 187–188 行；「開發流程慣例」第 1–4 條，約 217–220 行 | `docs/PLAN.md` 第 5 行、`docs/VOTE_2026-09-11.md` 的模型欄。`js/`、`scripts/` 裡的 `claude-opus-5` 是本 App AI 新聞摘要功能的計價表，**跟這一輪無關，不要動** |
| TripQuest | `CLAUDE.md` 第 3 條「代理投票與模型」（約 18–21 行）。STATUS：「工作慣例」第 1、2 條（約 118–134 行；第 2 條末尾「*已套用*：v1.66……七個代理全部改用 Opus 5 執行」是紀錄，保留，可加「（當時的模型設定）」）；「開發流程慣例」第 1–4 條（約 146–157 行）；「等 Yolin 回覆」④⑤ 的選項 B（約 92、99 行「開 3 個 Fable 代理」）；「環境與帳號注意事項」約 1324 行「先開 3 個 Fable 5.1 代理」那句。草稿 `docs/SPEC_WEB_PUSH.md`、`docs/SPEC_CREATOR_PERMISSIONS.md` 裡的「開 Fable 代理投票」 | STATUS 約 1081 行（v1.70.1）、約 1129 行、約 1206–1207 行的事後紀錄；`js/`、`workers/`、`scripts/` 裡 AI 功能用到的模型名 |
| MealMate | STATUS「常設規則」第 4 條（約 88 行）的署名範例寫死了 `Claude Opus 5`——改成不寫模型的寫法（例如「結尾加當下系統給的 `Co-Authored-By` 署名行」），以後換模型不必再改；「開發流程慣例」第 1–4 條（約 1584–1590 行）；「工作慣例」第 2、3 條（約 1602–1603 行） | STATUS 約 957 行「Fable 統籌 Session 定案」、`docs/PLAN.md` 第 5 行 |

**3. 順手做的（只做你那一列）：**

| App | 順手做 |
|---|---|
| JLPT_App | `CLAUDE.md` 第 14、20 行寫了磁碟機代號開頭的本機路徑（本 repo 與其他 App 的位置），跟你 STATUS「文件裡路徑一律用代稱」與新 §2.4 不符 → 改成不帶磁碟機的寫法（例如「本 repo」「同一層的其他 App：`../TripQuest` 等」） |
| StockDiary | `CLAUDE.md` 第 13 行寫了其他三個 App 的本機絕對路徑 → 改成相對路徑（`../TripQuest` 等） |
| TripQuest | `CLAUDE.md` 第 37 行寫了本 repo 的本機絕對路徑 → 改成「本 repo」。`docs/SPEC_任務只在有把握時產生.md` 檔頭還寫「定案，待執行」——它在 v1.74.0 已經做了，照檔頭的說明改成「已執行（2026-09-21、v1.74.0）」 |
| MealMate | `CLAUDE.md` 第 11 行寫了其他三個 App 的本機絕對路徑 → 改成相對路徑。`docs/SPEC_使用者自標葷素.md` 檔頭同上，改成「已執行（2026-09-21、v0.38.0）」 |

那幾行路徑早已在公開歷史裡，這次只是不讓它繼續出現在最新版；**不改寫 git 歷史**。

## 不准做的事

- 不改 `package.json`、`js/`、`sw.js`、`workers/`、`scripts/`、測試。
- `CLAUDE.md` 只准改第 2、3 步表裡你那一列列出的行；指向段不動。STATUS 只准改表裡列的條文與平常的收尾更新；有日期的歷史紀錄不改寫。
- 不必回頭盤點既有文件裡的個資（見「為什麼改版」第 2 點）。
- 不要動其他 App 與統籌工作區的檔案。條文若你覺得還是有問題：不要改副本，寫進回報。
- 不要自己更換本 Session 的模型或 effort。

## 驗證（結果貼進回報）

| # | 做什麼 | 合格條件 |
|---|---|---|
| U1 | `git status --short` | 只有：`docs/CONVENTIONS.md`（修改）、本工單（新增，請一起 commit）、`docs/STATUS.md`（修改），以及第 2、3 步你那一列提到的檔（`CLAUDE.md`、草稿、規格檔頭）。多一個都要解釋 |
| U2 | 副本第一行 | 逐字等於 `<!-- CONVENTIONS v5 2026-09-23 -->`，無 BOM、無 CR |
| U3 | 副本與附錄全文比對 | 逐字相同；比對方法要有對照組（故意改一個字應判成不同） |
| U4 | `git diff -- docs/CONVENTIONS.md` | 改動只在：第一行、開頭說明（「Fable 統籌 Session」→「統籌 Session」）、§0.3、§1、§2 第 4 條、§8 變更紀錄。其他條文零改動；有別的地方變了就是寫檔時走樣了，停下來查 |
| U5 | `git diff -- CLAUDE.md` | 只有你那一列列出的行；指向段（`## 共用慣例` 到檔尾）零改動；刪除行數＝新增行數（逐行改寫，不是整段搬動）——做不到就說明 |
| U6 | 殘留檢查：在 `CLAUDE.md` 與 `docs/STATUS.md` 跑表格下方那一條搜尋 | **改之前先跑一次，數字要大於 0**（對照組：搜尋式是活的）；改之後剩下的每一處，在回報裡寫行號、所在章節、一句「為什麼是歷史」 |
| U7 | 公開前自查（本 repo 是 public）：commit 之後、push 之前，對這次 commit 的新增行查四類——金鑰或 token、email（GitHub 與 Anthropic 的 noreply 不算）、本機使用者名稱（從環境變數取，不寫進檔案、不印）、磁碟機代號與家目錄路徑；**再用眼睛看一遍新增的文件行有沒有個資**（新 §2.4） | 四類零命中；每一類先確認對照組命中；**email 類的對照組用合成樣本**（在檢查當下組出一個假信箱字串餵給檢查器，不寫進任何檔案）。第 3 步刪掉的路徑會出現在「刪除行」，那是預期的；新增行裡必須零命中。**這一批自查通過就可以直接 push，不用等回覆** |
| U8 | 本 App 會讀文件的既有稽核 | StockDiary：`npm run doctest`、`npm run shelltest`；MealMate：`npm run doctest`；TripQuest：`npm run audittest`；JLPT_App：無。全綠 |
| U9 | `git log -1 --format='%ae'` | 是 GitHub noreply（回報時不要印出任何真實信箱） |

U6 的搜尋（整行照抄；六個樣式：Fable、claude-fable、後面不接 -5 的 claude-opus-5、後面不接 .5 的 Opus 5、大工程、切 Max）：

```bash
grep -nE 'Fable|claude-fable|claude-opus-5([^-]|$)|Opus 5([^.]|$)|大工程|切 ?Max' CLAUDE.md docs/STATUS.md
```

## commit 與回報

- 一個 commit，訊息以 `docs(conventions):` 開頭，例如 `docs(conventions): 副本更新到 v5；本 App 文件的模型設定跟著改`。push：JLPT_App 用 `CLAUDE.md` 裡那條專用指令；TripQuest 用 `git push origin main`；其餘用平常的方式。
- 回報：**最前面（有回執就放在回執之後）寫一行你這個 Session 實際跑的模型與 effort**（系統資訊裡看得到），順便確認 Yolin 的設定生效了；然後 commit hash、U1–U9 結果、第 2、3 步改了哪幾處（行號＋一句話）、新 §1 與 §2.4 跟本 App 現行規定有沒有牴觸（請直說）、工單沒預期到的狀況。
- **之後全新開場的 Session，回執應該寫 `已讀共用慣例 v5（2026-09-23）`。** 你這個 Session 不用補寫。

---

## 附錄：副本全文（`docs/CONVENTIONS.md` 的內容＝下面區塊裡的文字）

````markdown
<!-- CONVENTIONS v5 2026-09-23 -->
# 四個 App 共用的工作慣例（CONVENTIONS.md）

適用：JLPT_App、StockDiary、TripQuest、MealMate。
主檔在統籌工作區（統籌 Session 的 repo）；每個 App 的 `docs/CONVENTIONS.md` 是內容相同的副本。**不要直接改副本**，要改照 §7。
這裡只收四個 App 都同意的規則。每個 App 自己的紅線、指令、門檻在它的 `CLAUDE.md` 與 `docs/STATUS.md`。

## §0 怎麼用這份

1. 開工前讀完本檔、本 App 的 `CLAUDE.md` 與 `docs/STATUS.md`。
2. 第一則回覆的第一行寫回執：`已讀共用慣例 vN（日期）`——N 與日期抄本檔第一行，不要抄這個範例。沒有回執，Yolin 就當你沒讀。
3. **本 App 的 `CLAUDE.md` 與 `docs/STATUS.md` 優先於本檔。** 兩邊衝突時照較嚴的那一邊做，並在回報裡指出來，讓統籌者修本檔。只有 §2 第 1、2、4 條與 §3 是專案不得放寬的。
4. 讀不到本檔：先試 `git show HEAD:docs/CONVENTIONS.md`；還是沒有，就在第一則回覆第一行寫「讀不到共用慣例」，然後照本 App 的 `CLAUDE.md` 與 `docs/STATUS.md` 工作。

## §1 角色與流程（Yolin 2026-09-14 明訂；模型與 effort 由 Yolin 2026-09-23 改定）

1. **規劃：統籌 Session，Opus 5.5，effort Max。** 理解需求、拆解、寫成規格。單一 App 的規格放該 App 的 `docs/SPEC_<主題>.md`；跨 App 的放統籌工作區。（2026-09-23 之前，統籌者是 Fable 5.1、effort high，開發 Session 是 Opus 5、effort high；那段期間的規格、投票與決定保留原樣，屬當時的模型設定。）
2. **開發：Opus 5.5，平常 effort medium；重大任務可上調至 high。** 開發 Session 負責實作，也負責查找、蒐集資料。統籌者在規格檔頭寫建議的 effort，建議 high 的附理由；實際的模型與 effort 由 Yolin／Dispatch 設定，Session 不自行更換。舊規定「只有 Yolin 明講大工程才切 Max」2026-09-23 起作廢。
3. **流程**：統籌者寫 spec → 開發 Session 實作＋查資料 → 回報並逐項與 spec 對照 → 與 Yolin 確認。
4. **較大改動或值得討論的議題**：由統籌者開 3 個代理投票，同一份 prompt、各自獨立、多數決、分歧採保守；各代理當統籌，查資料交給勘查代理；統籌者彙整後回報。**投票代理與統籌者同模型、同 effort**，三個整批在同一個模型與 effort 上跑，結果才可比；投票紀錄寫明實際的模型 ID 與 effort，工具指定不了 effort 時寫「繼承統籌者設定」。哪些算「較大」、實作途中臨時遇到時怎麼辦，看本 App 的規定。
5. **模型或額度不可用時，不要停下來問。**（「不要停下來問」是 Yolin 2026-09-10 明訂）子代理（投票、勘查）因額度或速率限制（429／quota）失敗 → **整批改用同一個可用的模型重跑**（投票不可混用模型）→ 回報最前面（回執之後）用一行註明實際用了哪個模型與 effort、原因。連一個可用的模型都沒有才暫停。統籌者與各 Session 本身用哪個模型與 effort 由 Yolin 設定，Session 不自行更換。

## §2 授權的地板

1. **這三件一定先問 Yolin**：刪除或破壞使用者資料；花錢、或需要註冊帳號／綁卡的服務；動到本 App 以外的任何檔案。本 App 的投票或其他決策流程**不能取代**這一條：就算投票通過，只要做下去有刪除或破壞使用者資料的風險（例如改儲存格式、做資料遷移），動手前仍然先問。
2. **其他 App 的目錄可以讀、不可以寫**（讀來抄慣例、對照同一種 bug）。統籌 Session 只會在本 App 寫 `docs/SPEC_<主題>.md`；它不改程式、不 commit、不 push。
3. **本檔不規定的**：一般 commit／push／部署、改寫 git 歷史、force push、刪除已 commit 的檔案、建新 repo——看本 App 的 `CLAUDE.md` 與 `docs/STATUS.md`。那裡也沒寫的破壞性操作，一律當作要先問。
4. **金鑰與個資不進 repo。**（Yolin 2026-09-19：「只要不會有金鑰或個資上傳到公開就沒有問題」；四個 repo 都是 public，推上去就收不回來）要 commit 的內容不得出現：金鑰或 token、真實 email、本機使用者名稱、本機絕對路徑，以及 Yolin 與家人**真實的**個人資料——例如持股與金額、帳戶與交易、備份檔的內容、行程的日期與地點、家人的名字與健康或飲食狀況。規格或 Dispatch 要你拿真實資料驗證時，**寫進 repo 的只能是結論，或換成合成的數字**；原始資料不進 repo。自查腳本抓得到前四類、抓不到個資——push 前自己把新增的文件行看一遍。拿不準算不算，就當成是，在回報裡提出來。

## §3 溝通

1. **禁止互動式提示框／多選題工具（`AskUserQuestion` 之類）。**（Yolin 2026-09-16 明訂）需要 Yolin 決定的事一律用**純文字**寫在回覆裡：列出選項、每個選項的代價、你的建議與理由，然後**停下來等**，由 Dispatch 轉達。理由：Yolin 常從手機或另一台電腦遠端操作，提示框只渲染在本機那台 PC，遠端點不到，Session 就此卡死。
2. **全程繁體中文，包括思考與判斷的過程。**（Yolin 2026-09-16 明訂）維持原樣不硬翻：程式碼、變數與函式名、檔名與路徑、指令、專有名詞、引用出處。理由：Yolin 要能看懂你為什麼這樣決定。
3. **回報分開寫「實測驗證過的」與「推論的」。** 結尾放需要 Yolin 決定的事項，沒有就明講沒有。

## §4 給使用者看的文案要誠實

拿不到、算不出、還沒開放的，就照實寫（「尚未取得」「未估算」「這一版尚未開放」這類話）——不顯示 0、不用預設值冒充使用者輸入、不用舊資料冒充新資料。

## §5 測試紀律

1. **每條新斷言都要證明它會紅**：把對應邏輯改壞（突變），或拿修正前的程式跑，測試必須失敗；修回去必須綠。修 bug 時補一條「修正前會紅」的回歸斷言，並說明既有測試為什麼沒抓到。有新行為就補斷言。
2. **不寫假斷言。** 抓到過的形狀：母體是空的；母體恰好是「有問題的那幾個」；等待條件在上一頁就已經成立；反例太弱、被別條規則順便擋掉；掃描器把註解當程式碼；「應該是 0」旁邊沒有「母體非空」。寫完一條 `noneOf`／`everyOf` 就問一次：**這個母體現在有幾個元素？** 註解不是斷言。
3. **檢查器要有對照組**：餵已知該被抓到的輸入，斷言真的抓到。**斷言驗語意、不貼字面。** 對照組用自己造的合成樣本，不要借用會隨時間變的真實資料（實例：公開前自查的 email 對照組用 git 歷史的作者信箱，改用 noreply 之後就被排除規則濾掉、命中 0）。
4. **從使用者實際會走的入口測**：每個功能至少一條斷言從真實入口（畫面上的按鈕、對外的函式）進去，不能只驗內層純函式。
5. **含 regex 或反斜線的測試碼、補丁，一律用 Write／Edit 工具直接寫檔，不要經過 shell heredoc 或 `node -e`**：`\s` 變 `\\s` 或被吃掉都不報錯、測試照樣綠、但永遠不命中。
6. **打真網路的測試不進 `npm test`**，另開獨立指令；對外部服務的請求頻率照該服務的規矩。
7. **不必每一版都跑全套測試：平常跑受影響的就夠，全面檢測由 Yolin 定期指定。**（Yolin 2026-09-12 對全部專案的指示；2026-09-19 重申：「不用每一次小改動都全測，我會定期進行全面檢測」）這一條是**解除義務，不是設上限**——想多跑、想跑全套，永遠可以，不需要誰同意；本 App 規定每版要跑哪些的，照本 App 的規定。怎麼判定「受影響」由各 App 自己定。沒有放寬的那一條：新斷言仍然必須證明會紅。
   **耗時很久的整套（突變整套、全面檢測）**：等一批交辦事項做完再一起跑、一起看（Yolin 2026-09-19），不必每版跑、也不照固定天數。由 Yolin 指定；Session 不必自己跑（想跑永遠可以）。各 App 在自己的 STATUS 寫明「整套」指什麼、實測要跑多久、以及上限（距上次整套幾版、幾條突變或新斷言從未整套跑過）——上限照本 App 整套的成本訂：要跑幾小時的訂緊一點；只要幾分鐘的可以訂得很寬，或乾脆寫「每版都跑整套、不需要上限」。耗時還沒量過就寫「待量」，不要猜；超過上限時，回報最前面用一行講出來並建議「這一批做完就跑」，沒超過不必每次提。**成本算對地方**：整套是機器在背景跑，花的是時間，不是 token；花 token 的是處理結果的來回——所以要省的是「處理的次數」，結果一次回報、依根因分組。回報寫明是在哪個 commit 上跑的；沒紅的先在最新版複驗再處理。**刪一條突變之前，要能說出這段程式為什麼在任何真實路徑上都走不到**；說不出來，就是測試走錯路，要改的是測試。
8. 隨機或情境敏感的輸出（排程、排菜、抽樣）不得綁死單一結果。需要特定情境才分得出對錯的斷言：優先自己造情境、直接呼叫被測的函式；若用整段產生的結果，先寫一條前置斷言確認情境存在，不成立就紅。**固定 seed 不算釘住情境**（資料或規則一改，同一個 seed 的結果就變）；沒有前置的「至少 1 個」就是假斷言。前置斷言自己也要用突變證明會紅（把情境拿掉，它必須紅）。統計型斷言另照各 App 的做法（多組種子量餘裕）。沒有單元測試層、只有端對端測試的 App，「自己造情境」就用它造得出來的方式（預置儲存狀態、URL 參數）；前置斷言照樣要。

## §6 文件

1. **`docs/STATUS.md` 是接手契約。** 最前面要有「目前進行中／交接」與「等 Yolin 回覆」這兩塊（叫什麼名字、是獨立一節還是小節，照本 App 現有的寫法）；這兩塊過期了就改寫，不要往下追加。只存在對話裡的決定、規則、踩坑，收尾前寫進 STATUS。
2. 規格 `docs/SPEC_<主題>.md`；投票紀錄 `docs/VOTE_<日期>.md`。
3. 文件裡的數字盡量由靜態稽核從程式數出來比對；文件會漂移，而漂移不會讓任何測試變紅。

## §7 本檔怎麼改

1. **一條規則該進本檔還是留專案**，依序問：(a) 把規則裡的 App 名、檔名、數字換成另一個 App 的，還成立且措辭不必改嗎？(b) 在任何一個 App 違反它，Yolin 都會認為做錯了嗎？(c) 它是「原則＋某 App 的執行細節」嗎？——(a)(b) 都是 → 原則進本檔，細節留專案。領域紅線（健康、投資、使用者資料）永遠留專案；Yolin 指名「全部專案」的直接進本檔。
2. **只有統籌 Session 改主檔**：第一行版本 +1、日期改當天、§8 加一行。各 App 的副本由該 App 自己的 Session 依統籌者寫的工單更新。副本落後不擋任何工作。
3. 開發 Session 發現值得變成通則的規則：先照常寫進本 App 的 STATUS，並在回報裡標「候選通則」，由 Dispatch 轉給統籌者。不要改副本。
4. Yolin 明訂的直接進；Session 提議的要 Yolin 點頭；會改變四個 App 做法的才開 3 代理投票。

## §8 變更紀錄

- v5（2026-09-23）：兩處，都是 Yolin 親口定的，沒有開投票。**§1 改成 Yolin 2026-09-23 的模型政策**，原話：「之後統籌者為 Opus 5.5 + effort Max，其餘 Session 為 Opus 5.5 + effort medium，若有重大任務可上調至 high」——統籌者 Opus 5.5／Max；開發 Session Opus 5.5／medium，重大任務可上調 high（統籌者在規格檔頭建議、Yolin／Dispatch 設定）；投票代理與統籌者同模型同 effort、紀錄寫明模型 ID 與 effort；舊的「Fable 額度用盡改用 Opus 5」前提（統籌者是 Fable）已不成立，改寫成不綁特定模型的「整批改用同一個可用的模型、不要停下來問」，它帶著的兩個原則（不要停下來問、投票整批同模型）保留。**§2.4 新增「金鑰與個資不進 repo」並列入 §0.3 不得放寬**：Yolin 2026-09-19 就說過（「只要不會有金鑰或個資上傳到公開就沒有問題」），主檔一直沒寫；2026-09-21 StockDiary 照規格拿真實資料驗證、把結果照抄進 STATUS，個人財務資料差點推上 public repo（push 前的驗收攔下）。開頭說明裡的「Fable 統籌 Session」改成「統籌 Session」。
- v4（2026-09-21）：四處，都來自四個 App 做 v3 更新時的回報，沒有新政策。§0.2 回執範例改成不含版本的寫法（v3 升版時統籌者漏改範例，TripQuest、StockDiary、JLPT_App 各自抓到）。§5.3 補「對照組用合成樣本」（StockDiary：改用 noreply 後，拿作者信箱當自查對照組會失效）。§5.7 補「寫明整套是什麼、實測多久；上限照成本訂；沒量過寫待量」（四個 App 的整套從 222 秒到 4.1 小時，差三個數量級；v3 是照最貴的那個寫的）。§5.8 替只有端對端測試的 App 留口（JLPT_App）。
- v3（2026-09-21）：§5.7 補「耗時很久的整套」一段、§5.8 改寫。起因：Yolin 2026-09-19 問「耗時很久的突變不用這麼頻繁做，等手邊的待改事項完成後再一起檢視」；同一天 MealMate 全面檢測抓到 3 條「情境消失、斷言照樣綠」——§5.8 原文早就要求前置斷言，但沒講「固定 seed 不算釘住情境」。3 代理投票六題皆 3:0（§5.8 改寫但要短；整套的時機進共用、上限數字留各 App；操作細節留各 App，只進「刪突變前先確認真實路徑」與「成本寫明」）。代理補的三點已納入：前置斷言也要突變驗紅、「Session 不自己跑」與「解除義務不是設上限」矛盾故改成「不必」、回報寫明 commit 並在最新版複驗。
- v2（2026-09-19）：三處措辭修正，都來自四個 App 導入時的回報。§5.7 改寫——原文讀起來像在限制跑全面檢測，JLPT_App 與 MealMate 都因此誤讀或列為矛盾；現在明講是解除義務、不是設上限。§2.1 補一句：投票不能取代「刪除或破壞使用者資料先問」（JLPT_App 回報它的投票制與這一條有交集；統籌者決定地板優先，Yolin 可以否決）。§6.1 那兩塊的名稱與層級照各 App（StockDiary、JLPT_App 回報結構不同）。
- v1（2026-09-19）：初版。只收四個 App 現有條文的交集。規格：統籌工作區的 `SPEC_共用慣例.md`（v2）。
````
